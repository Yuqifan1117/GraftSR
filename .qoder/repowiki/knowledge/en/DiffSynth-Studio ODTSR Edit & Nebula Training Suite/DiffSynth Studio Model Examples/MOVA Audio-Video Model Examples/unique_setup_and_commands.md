Datasets are downloaded via `modelscope download --dataset DiffSynth-Studio/diffsynth_example_dataset --include "mova/MOVA-360P-I2AV/*" --local_dir ./data/diffsynth_example_dataset`. Full training uses `examples/wanvideo/model_training/full/accelerate_config_14B.yaml`; LoRA training uses default accelerate config. Two-phase training splits the timestep range at boundary 0.358 (timesteps [900,1000] for high noise, [0,900) for low noise).